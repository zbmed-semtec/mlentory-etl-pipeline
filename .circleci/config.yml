version: 2.1

# machine:
#   image: ubuntu-2004

jobs:
  build:
    executor: docker
    steps:
      - checkout
      - working_directory: ./Backend
      - docker-compose pull
      - docker-compose up -d
      - docker-compose logs -f

workflows:
  build-and-test:
    jobs:
      - build

      